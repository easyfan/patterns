{
  "skill_name": "patterns",
  "evals": [
    {
      "id": 1,
      "prompt": "/patterns",
      "expected_output": "列出 ~/.claude/patterns/ 目录下所有可用 pattern，含名称和修改时间",
      "assertions": [
        {
          "text": "输出包含 '📚 可用 Pattern 列表' 标题",
          "check": "contains('📚 可用 Pattern 列表')"
        },
        {
          "text": "输出包含 'agent-monitoring' pattern 条目",
          "check": "contains('agent-monitoring')"
        },
        {
          "text": "每个条目包含修改时间（含'修改:'字样）",
          "check": "contains('修改:')"
        }
      ]
    },
    {
      "id": 2,
      "prompt": "/patterns",
      "expected_output": "列出 pattern 时包含用法提示，不触发 /skill-review 询问",
      "assertions": [
        {
          "text": "输出包含用法示例（'/patterns <name>' 或 '--patch'）",
          "check": "contains('/patterns') || contains('--patch')"
        },
        {
          "text": "列出模式不触发 /skill-review 询问（无质量门）",
          "check": "!contains('是否对新创建的文件执行 /skill-review')"
        }
      ]
    },
    {
      "id": 3,
      "prompt": "/patterns nonexistent_pattern_xyz_12345",
      "expected_output": "输出提示找不到该 pattern，然后列出可用 pattern，不崩溃",
      "assertions": [
        {
          "text": "输出不含 Error 或 Traceback",
          "check": "!contains('Traceback') && !contains('Error:')"
        },
        {
          "text": "输出包含可用 pattern 列表引导（含 'agent-monitoring' 或 '📚 可用 Pattern 列表'）",
          "check": "contains('agent-monitoring') || contains('📚 可用 Pattern 列表')"
        }
      ]
    },
    {
      "id": 4,
      "prompt": "/patterns --patch",
      "expected_output": "扫描所有含 generated-from 字段的已实例化命令，若无则输出提示，不崩溃",
      "assertions": [
        {
          "text": "输出包含 patch 扫描相关内容（如'发现'、'修补'、'命令'、'已实例化'、'generated-from'等关键词之一）",
          "check": "contains('修补') || contains('发现') || contains('generated-from') || contains('已实例化') || contains('patch')"
        },
        {
          "text": "不崩溃（不含 'Traceback'）",
          "check": "!contains('Traceback')"
        }
      ]
    },
    {
      "id": 5,
      "prompt": "/patterns --patch research-module",
      "expected_output": "扫描指定命令，若未找到则输出提示，不崩溃",
      "assertions": [
        {
          "text": "输出 '🔍 发现以下已实例化命令' 或 '未找到可修补的命令文件' 之一",
          "check": "contains('🔍 发现') || contains('未找到可修补')"
        }
      ]
    },
    {
      "id": 6,
      "prompt": "/patterns",
      "expected_output": "每次列出 pattern 时末尾有使用建议",
      "assertions": [
        {
          "text": "输出包含 '📚 可用 Pattern 列表'",
          "check": "contains('📚 可用 Pattern 列表')"
        },
        {
          "text": "包含 --patch 使用提示",
          "check": "contains('--patch')"
        }
      ]
    },
    {
      "id": 7,
      "prompt": "/patterns --patch nonexistent_cmd_xyz_99999",
      "expected_output": "指定命令名不存在时，--patch 扫描返回"未找到"提示，不崩溃，不含 Traceback",
      "assertions": [
        {
          "text": "输出包含'未找到可修补的命令文件'或'🔍 发现'之一（找到匹配或给出无结果提示）",
          "check": "contains('未找到可修补') || contains('🔍 发现')"
        },
        {
          "text": "不崩溃（不含 'Traceback'）",
          "check": "!contains('Traceback')"
        }
      ]
    }
  ]
}
